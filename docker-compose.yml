services:
  frontend:
    build: ./frontend
    command: npm run dev -- --host
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules  # <- isso mantém os node_modules do container
    depends_on:
      - mock-backend
  
  # backend:
  #   build: ./backend
  #   container_name: backend
  #   ports:
  #     - "4000:4000"
  #   # env_file:
  #   #   - ./backend/src/.env
  #   volumes:
  #     - db_data:/app/database

  mock-backend:
    build: ./backend
    #command: node mock-ws-server.js
    ports:
      - "4000:4000"
    # volumes:
    #   - ./backend:/app

volumes:
  db_data:
    driver: local



    